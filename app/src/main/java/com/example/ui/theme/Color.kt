package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// "Immersive UI" Dark Theme Color Schema
val DarkBackground = Color(0xFF0B0E11)      // Jet black base
val DarkSurface = Color(0xFF1F2124)         // Sleek slate grey container
val DarkSurfaceVariant = Color(0xFF2D3035)  // Slightly lighter slate for contrast
val DarkBorder = Color(0xFF2D3035)          // Clean dark border line

val AccentCyan = Color(0xFFA8C7FF)          // Soft dynamic blue
val AccentPurple = Color(0xFFB388FF)        // Ambient glowing violet/indigo
val AccentIndigo = Color(0xFFD2E3FF)        // High contrast primary baby blue
val StatusOnline = Color(0xFF34D399)        // Immersive active green indicator
val ErrorRadical = Color(0xFFFF5252)        // Soft beautiful warning red

val BubbleMe = Color(0xFF003062)            // Deep tech blue me bubble
val BubbleThem = Color(0xFF2D3035)          // Structured dark surface bubble for interlocutor

val TextPrimary = Color(0xFFE3E2E6)         // Cozy off-white primary text
val TextSecondary = Color(0xFFC4C7C5)       // Soft medium contrast text
val TextMuted = Color(0xFF939094)           // Muted placeholder text
